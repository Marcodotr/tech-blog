<section class="container-fluid">  
    <div class="p-5 mb-4 bg-light rounded-3">
        <div class="container-fluid py-5">
            <h1 class="display-5 fw-bold">Add post</h1>
            <form class="form d-flex justify-content-between addPost">
                <div class="form-floating col-2">
                    <input type="text" name="title" class="form-control" id="title" placeholder="Title">
                    <label for="ptFi">Title</label>
                </div>
                <div class="form-floating col-6">
                    <input type="text" name="body" class="form-control" id="body" placeholder="Body Of Post">
                    <label for="ptLastName">Body Of Post</label>
                </div>
                <button class="col-1 btn btn-lg btn-primary" id="addPT">Add Post</button> 
            </form>
        </div>
    </div>

    {{#each post as |post|}}
    {{> blogpost}}
    {{/each}}

          <div class="col-md-6">
            <div class="h-100 p-5 bg-light border rounded-3">
              <h2>Add borders</h2>
              <p>Or, keep it light and add a border for some added definition to the boundaries of your content. Be sure to look under the hood at the source HTML here as we've adjusted the alignment and sizing of both column's content for equal-height.</p>
              <button class="btn btn-outline-secondary" type="button">Example button</button>
            </div>
          </div>
    </div>
</section>
<script src="/js/post.js"></script>